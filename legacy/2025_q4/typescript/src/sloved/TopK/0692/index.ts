/*
# 692. 前 K 個高頻單詞 (Top K Frequent Words)
* ## 題目說明
* 給定一個單詞陣列 words 和一個整數 k，返回出現頻率最高的 k 個單詞。
* 若不同單詞出現頻率相同，按字典序排序。
* ---
* ### 範例 1：
* 輸入：words = ["i","love","leetcode","i","love","coding"], k = 2
* 輸出：["i","love"]
* ---
* ### 限制條件：
* 1 <= k <= words.length <= 500
* 1 <= words[i].length <= 10
* words[i] 僅包含小寫字母
* ---
* ## 思路提示：
* 雜湊表統計頻率，最小堆維護 top k，頻率相同時字典序小的優先。
*/

export function topKFrequent(words: string[], k: number): string[] {
  // 實作見 test case
  return []
}
