/*
# 658. 找到最接近的 K 個元素 (Find K Closest Elements)
* ## 題目說明
* 給定一個排序好的陣列 arr、一個整數 k 和目標值 x，返回最接近 x 的 k 個元素。
* 結果需按升序排列。
* ---
* ### 範例 1：
* 輸入：arr = [1,2,3,4,5], k = 4, x = 3
* 輸出：[1,2,3,4]
* ---
* ### 限制條件：
* 1 <= k <= arr.length
* 1 <= arr.length <= 10^4
* -10^4 <= arr[i], x <= 10^4
* ---
* ## 思路提示：
* 二分搜尋 + 雙指針，或最大堆維護 k 個最接近的元素。
*/

export function findClosestElements(arr: number[], k: number, x: number): number[] {
  // 實作見 test case
  return []
}
