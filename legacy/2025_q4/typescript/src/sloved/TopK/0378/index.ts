/*
# 378. 有序矩陣中第 K 小的元素 (Kth Smallest Element in a Sorted Matrix)
* ## 題目說明
* 給你一個 n x n 的整數矩陣 matrix，矩陣每行每列皆遞增排序。
* 請返回矩陣中第 k 小的元素。
* ---
* ### 範例 1：
* 輸入：matrix = [[1,5,9],[10,11,13],[12,13,15]], k = 8
* 輸出：13
* ---
* ### 限制條件：
* 1 <= n <= 300
* n^2 >= k >= 1
* -10^9 <= matrix[i][j] <= 10^9
* ---
* ## 思路提示：
* 可用最小堆合併每行，或二分搜尋。
*/

export function kthSmallest(matrix: number[][], k: number): number {
  // 實作見 test case
  return 0
}
