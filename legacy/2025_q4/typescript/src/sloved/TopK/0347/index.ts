/*
# 347. 前 K 個高頻元素 (Top K Frequent Elements)
* ## 題目說明
* 給定一個非空整數陣列 nums，返回其中出現頻率前 k 高的元素。
* ---
* ### 範例 1：
* 輸入：nums = [1,1,1,2,2,3], k = 2
* 輸出：[1,2]
* ---
* ### 限制條件：
* 1 <= k <= nums.length <= 10^5
* nums[i] 是整數
* ---
* ## 思路提示：
* 可用雜湊表統計頻率，再用最小堆維護前 k 高頻元素。
* 時間複雜度 O(n log k)
*/

export function topKFrequent(nums: number[], k: number): number[] {
  // 實作見 test case
  return []
}
