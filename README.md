# my_leetcode_solved

這是我的 LeetCode 解題總庫，依語言分資料夾。

- `csharp/`: C# 解題專案，詳細規則看 `csharp/README.md`
- `py/`: Python 解題，題目資料夾用 `lc####/`，解答在 `sol.py`，測試在 `test.py`
- `typescript/`: TypeScript 解題，題目在 `src/sloved/lc####/`，解答 `index.ts`，測試 `index.test.ts`
- `legacy/`: 舊資料，只查考，不優先新增

AI 注意：忽略 `.git/`、`.vs/`、`node_modules/`、`bin/`、`obj/`、`__pycache__/`。新增或修改題解時，照各語言既有命名方式，並同步補測試。
