/*
# 295. 資料流中位數 (Find Median from Data Stream)
* ## 題目說明
* 設計一個類 MedianFinder，支援動態新增數字並能隨時取得目前所有數字的中位數。
* ---
* ### 範例 1：
* 操作：addNum(1), addNum(2), findMedian()
* 輸出：1.5
* 操作：addNum(3), findMedian()
* 輸出：2
* ---
* ### 限制條件：
* -10^5 <= num <= 10^5
* 最多呼叫 5 * 10^4 次 addNum 和 findMedian
* ---
* ## 思路提示：
* 用最大堆和最小堆分別維護左右兩半，保證平衡。
*/

export class MedianFinder {
  constructor() {
    // 初始化
  }
  addNum(num: number): void {
    // 實作見 test case
  }
  findMedian(): number {
    // 實作見 test case
    return 0
  }
}
