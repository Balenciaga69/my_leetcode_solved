/*
# 973. 最接近原點的 K 個點 (K Closest Points to Origin)
* ## 題目說明
* 給定一個 points 陣列，points[i] = [xi, yi] 代表平面上的一個點。
* 請返回距離原點 (0,0) 最近的 k 個點。
* ---
* ### 範例 1：
* 輸入：points = [[1,3],[-2,2]], k = 1
* 輸出：[[-2,2]]
* ---
* ### 限制條件：
* 1 <= k <= points.length <= 10^4
* -10^4 <= xi, yi <= 10^4
* ---
* ## 思路提示：
* 用最大堆維護 k 個最近的點，或 QuickSelect。
*/

export function kClosest(points: number[][], k: number): number[][] {
  // 實作見 test case
  return []
}
