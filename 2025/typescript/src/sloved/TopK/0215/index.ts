/*
# 215. 陣列中的第 K 大元素 (Kth Largest Element in an Array)
* ## 題目說明
* 給定一個整數陣列 nums 和一個整數 k，請你返回陣列中第 k 個最大的元素。
* 請注意，你需要找的是陣列排序後的第 k 個最大的元素，而不是第 k 個不同的元素。
* ---
* ### 範例 1：
* 輸入：nums = [3,2,1,5,6,4], k = 2
* 輸出：5
* ---
* ### 限制條件：
* 1 <= k <= nums.length <= 10^5
* -10^4 <= nums[i] <= 10^4
* ---
* ## 思路提示：
* 可用最小堆維護 k 個最大元素，或 QuickSelect。
* 時間複雜度 O(n)（平均），空間 O(k)
*/

export function findKthLargest(nums: number[], k: number): number {
  // 實作見 test case
  return 0
}
